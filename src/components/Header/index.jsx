import { HeaderWrapper, Title } from "@/components/Header/styled";

export const Header = () => {
    return (
        <HeaderWrapper>
            <Title>Toast demo</Title>
        </HeaderWrapper>
    );
};
